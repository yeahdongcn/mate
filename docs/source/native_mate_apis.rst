:orphan:

Native `mate` APIs
==================

This overview has been folded into :doc:`api_reference` so the fallback path
and the symbol-level reference live in one place.

Use :doc:`api_reference` for native ``mate`` API entrypoints and callable
details. If the integration fails at runtime, continue with
:doc:`diagnostics`.
